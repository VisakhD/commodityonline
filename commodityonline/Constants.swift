//
//  File.swift
//  commodityonline
//
//  Created by Netcom on 04/08/22.
//

import Foundation


struct k {
    static let networkError = "networkerror"
    static let phoneNumber = +918448090300
    static let loadingPage = "loading"
    static let mainURL = "https://www.commodityonline.com/?utm_source=mobile-app&utm_campaign=mobile-app"
    static let watzURL = "https://api.whatsapp.com/send?phone=\(phoneNumber)"
    static let loadScreen = "pageNotFound"
    static let phone = "tel"
    static let mail = "mailto"
    static let fb = "facebook.com"
    static let insta = "instagram.com"
    static let twit = "twitter.com"
    static let apple = "apps.apple.com/in"
    static let watz = "whatsapp.com"
    
}















//        else if navigationAction.navigationType == .linkActivated {
//                if let url = navigationAction.request.url,
//                    let host = url.host, !host.hasPrefix("facebook.com"),
//                    UIApplication.shared.canOpenURL(url) {
//                    UIApplication.shared.open(url, options: [:])
//                    print(url)
//                    print("No need to open it locally")
//                    decisionHandler(.cancel)
//                }
//        }
////            if let url = navigationAction.request.url ,UIApplication.shared.canOpenURL(url){
////                if (url.absoluteString.range(of: "facebook.com") != nil || url.absoluteString.range(of: "twitter.com") != nil || url.absoluteString.range(of: "instagram.com") != nil
////                    || url.absoluteString.range(of: "googleplay.com") != nil){
////                    UIApplication.shared.open(url, options: [:])
//////                    decisionHandler(.cancel)
////            }
////        }
////            let url = navigationAction.request.url!
////            UIApplication.shared.canOpenURL(url)
////            decisionHandler(.cancel)
////        }
////
////        else if navigationAction.request.url?.scheme == "wa.me" {
////            if UIApplication.shared.canOpenURL(appURL) {
////                if #available(iOS 10.0, *) {
////                    UIApplication.shared.open(appURL, options: [:], completionHandler: nil)
////                }
////                else {
////                    UIApplication.shared.openURL(appURL)
////                }
////            }
////
////            decisionHandler(.cancel)
////
////        }
   








